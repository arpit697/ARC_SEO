import { PaginationStyleDirective } from './pagination-style.directive';

describe('PaginationStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new PaginationStyleDirective();
    expect(directive).toBeTruthy();
  });
});
